schema_version: 1

context:
  python_min: "3.10"
  name: cinnabar
  version: "0.6.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/OpenFreeEnergy/${{ name }}/archive/refs/tags/${{ version }}.tar.gz
  sha256: 25455158cb70686394d98a810f7b5b1445eae95a44e69d2cb1b645cf9a69d155

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv
  python:
    entry_points:
      - cinnabar = cinnabar.arsenic:main

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - setuptools >=61.0
  run:
    - adjusttext
    - matplotlib-base
    - networkx
    - numpy <2.4.0  # https://github.com/OpenFreeEnergy/cinnabar/issues/198
    - openff-units
    - plotly
    - pydantic >=1.10.17
    - python >=${{ python_min }}
    - scikit-learn
    - seaborn

tests:
  - python:
      imports:
        - cinnabar
      pip_check: true
      python_version: ${{ python_min }}
  - requirements:
      run:
        - pip
        - pytest
        - python ${{ python_min }}.*
    script:
      - pytest --pyargs cinnabar

about:
  license: MIT
  license_file: LICENSE
  summary: Consistent presentation of free energy data
  homepage: https://github.com/OpenFreeEnergy/cinnabar

extra:
  recipe-maintainers:
    - jthorton
    - mikemhenry
    - IAlibay
    - dwhswenson
    - RiesBen
    - atravitz
